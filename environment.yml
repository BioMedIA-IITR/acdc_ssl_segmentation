name: acdc_ssl
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - numpy
  - scipy
  - scikit-image
  - matplotlib
  - pyyaml
  - tqdm
  - tensorboard
  - pip:
      - torch
      - torchvision
      - nibabel
