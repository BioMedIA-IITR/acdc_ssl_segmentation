data_dir: /path/to/ACDC/training
output_dir: ./runs/acdc_ssl
image_size: 256
num_clusters: 4
base_channels: 32
epochs: 100
batch_size: 8
learning_rate: 0.0001
num_workers: 4
seed: 42
save_every: 10

recon_weight: 1.0
consistency_weight: 0.5
tv_weight: 0.05
entropy_weight: 0.01
balance_weight: 0.1
